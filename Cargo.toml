[workspace]
resolver = "2"

# This repository is a mono-repo that currently primarily hosts non-Rust
# projects/docs. Keep an empty workspace so `cargo` commands don't error.
members = ["tools/placeholder-rust"]

